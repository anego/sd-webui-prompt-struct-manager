# Global Rules & Routing

コードを生成・編集・レビューする際は、対象のファイル拡張子に応じて必ず以下のルールファイルを事前に読み込み、遵守すること。

- **`.vue` を編集する場合**:
  => 必須読込: `.claude/rules/vue-typescript.md`, `.claude/rules/vue-template.md`, `.claude/rules/vue-style.md`
- **`.ts` および `.d.ts` を編集する場合**:
  => 必須読込: `.claude/rules/vue-typescript.md`, `.claude/rules/typescript-types.md`
- **`.py` を編集する場合**:
  => 必須読込: `.claude/rules/python-backend.md`
- **テストコードを書く場合**:
  => 必須読込: `.claude/rules/testing.md`
- **`.vue` および `.ts` および `.d.ts` を編集する場合**
  => 必須読込: `.claude\rules\javascript-log.md`
