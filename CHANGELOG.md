# 変更履歴 (Changelog)

このプロジェクトの主な変更点を記録します。

## [1.4.4] - 2026-08-20

### 追加 (Added)

- **プロンプトのホバーツールチップ**: プロンプトにカーソルを合わせると、名前・プロンプト内容・メモを一覧できるツールチップを表示するようにしました。値が設定されている項目のみ表示され、項目間は点線で区切られます。

### 変更 (Changed)

- **編集(鉛筆)アイコンのタップ領域拡大**: プロンプト右側の編集アイコンが小さくクリックしづらかったため、常時見える円形の背景付きボタンに変更し、クリック領域を広げました。

### 修正 (Fixed)

- **検索フィルター解除時にグループの開閉状態が復元されない問題**: 検索フィルターで一致したグループは自動的に展開されますが、フィルターを解除した際に、フィルター使用前は閉じていたグループが開いたままになってしまう不具合を修正しました。フィルター開始時に各グループの開閉状態をスナップショット保存し、フィルター解除時にその状態へ復元するようにしました（Positive / Negative の各ペインで独立して動作します）。

## [1.4.3] - 2026-08-07

グループ単位の「ロック」と「非表示」機能を追加しました。整理が完了したグループを誤操作から守ったり、普段編集しないグループをツリーから隠して見通しをよくしたりできます。

### アップグレード時の注意

- **`npm run build`（または `pnpm build`）でのビルドが必要です。** フロントエンドのみの変更で、Python側のAPI追加はありません（WebUIの再起動は不要です）
- 既存のプロンプトYAMLへの影響はありません。新しいフラグ（`isLocked` / `isHidden`）はいずれも省略可能で、未設定の既存データはそのまま動作します

### 追加 (Added)

- **グループロック**: グループヘッダーの「ロック」スイッチ（赤色）をONにすると、そのグループ自身および配下の全アイテム（サブグループ含め再帰的）が一切編集できなくなります
  - 有効/無効の切り替え、内容・重みの編集、複製・削除、ドラッグ&ドロップによる移動・並べ替え、移動先ダイアログでの移動、新規アイテムの追加、サブ分類でのグループ化、択一/ランダムモードやカテゴリー・ヘッダー色などのグループ設定変更、キーボードショートカット（Space/F2/Delete/Insert）まで、すべてがロック対象です
  - 親グループをロックすると配下のサブグループ・プロンプトすべてに継承されます。ロック解除は必ず一番外側の祖先グループから順に行います
  - ロック中のグループは、他のアイテムを移動する際の移動先候補からも除外されます
- **グループ非表示**: 普段編集しないグループをツリーから隠せます。グループのホバー時アクションアイコン（目のアイコン）または右クリックメニューの「非表示にする」から設定できます
  - 各ペイン（Positive / Negative）の検索フィルター横にある目のアイコンで「非表示グループの表示」をON/OFFでき、非表示グループをまとめて再表示・再び隠せます（Positive/Negative共通のグローバル設定で、ブラウザに保存され再読み込み後も保持されます）
  - 検索フィルターに文字を入力した場合は、「非表示グループの表示」がOFFでも非表示グループの中身が検索対象に含まれるため、隠した場所を忘れても検索で見つけられます
  - ロック中のグループは非表示状態も変更できません（他のグループ設定変更と同様の扱いです）

## [1.4.2] - 2026-08-01

Checkpoint・Sampling Steps等のWebUI生成設定を、プロンプトとは別に名前を付けて保存・呼び出せる「生成設定プロファイル」機能を追加しました。

### アップグレード時の注意

- **`npm run build`（または `pnpm build`）でのビルドが必要です。** フロントエンドの変更が含まれます
- **WebUIの再起動が必要です。** Python側に新しいAPIエンドポイント（`/psm/generation-profiles`）が追加されています
- 既存のプロンプトYAML・設定ファイルへの影響はありません。新機能のデータは新規ファイル（`generation_profiles.json`）に保存されます

### 追加 (Added)

- **生成設定プロファイル**: Checkpoint / Sampling Steps / CFG Scale / Width / Height / Seed のうち保存したい項目を選び、名前を付けて保存できます
  - Sampling Method / Schedule Type / VAE・Text Encoderは、WebUI側のドロップダウン実装の都合上、確実な自動反映ができないため、そもそも保存対象の項目には含めていません
  - **保存と適用の導線を分離**: ツールバーに保存済みプロファイルを選ぶだけで即座に適用されるプルダウンを新設し、隣の保存ボタン（項目選択チェックボックス付きダイアログ）・削除ボタンと役割ごとに分けました（既存の「プロファイル」機能と同じ配置パターン）
  - **項目を選んで保存**: プロファイルごとに保存する項目をチェックボックスで自由に選択できます（例: Checkpointだけ固定したいプロファイル、生成パラメータ一式を固定したいプロファイル、など）
  - **独立した保存先**: プロンプトのYAMLファイルとは完全に別の `generation_profiles.json` に保存されるため、どのプロンプトファイルを開いていても同じ生成設定プロファイル一覧を使えます
  - **WebUIへの適用**: プルダウンで選択した瞬間に、保存済みの値がすべてWebUIへ反映されます

## [1.4.1] - 2026-07-26

v1.4.0 で追加したドラッグ&ドロップ機能の不具合を修正したパッチリリースです。

### 修正 (Fixed)

- **グループ移動時にドロップエリアが想定外に広がる問題**: 閉じたグループのドロップゾーンが配下のプロンプトをすべて描画していたため、アイテム数の多いグループではドロップエリアが縦横に展開し、プロンプトの文字列がはみ出して表示されていました。ドロップゾーン内の要素は非表示にし、高さが常に一定に保たれるようにしました（`max-height` と `overflow: hidden` による保険も追加）

## [1.4.0] - 2026-07-25

Animaモデル対応を軸に、プロンプト翻訳・PNG Info取込・自動グループ化を追加した大型アップデートです。
あわせてドラッグ&ドロップと移動操作を作り直し、日々の整理作業を軽くしました。

### ハイライト

1. **Animaモデルに対応** — ファイル単位でモードを切り替え、Anima推奨のタグ順序で自動整列。テンプレートから始められます
2. **日本語で書いて英語で出力** — ローカルLLM（LM Studio / Ollama）またはクラウドAPIで自然言語プロンプトを英訳
3. **PNG Infoから一発取込** — 生成画像のパラメータを貼るだけで、重み・LoRA・BREAKを保ったままツリー化
4. **タグの自動グループ化** — タグDBとキーワードルールで髪・服装・ポーズなどへ自動振り分け（AI補完も可）
5. **整理作業の操作性を改善** — ドラッグ中にレイアウトがずれないクローン方式、検索付きの移動ダイアログ、反映前プレビュー

### アップグレード時の注意

- **`pnpm build`（または `npm run build`）でのビルドが必要です。** フロントエンドの変更が含まれます
- **WebUIの再起動が必要です。** Python側に新しいAPIエンドポイントが追加されています（プロセスごと再起動してください）
- **既存のYAMLはそのまま使えます。** 追加フィールドはすべて省略可能で、未設定のファイルは従来どおり `SD系` として動作します
- 一部の機能は任意の連携拡張が必要です（未導入でも他機能は通常どおり動作します）
  - 自動グループ化・カテゴリ判定 → [a1111-sd-webui-tagcomplete](https://github.com/DominikDoom/a1111-sd-webui-tagcomplete)
  - 翻訳・タグ名の日本語化・AI分類 → LM Studio / Ollama 等（OpenAI互換API）または DeepL API

### 追加 (Added)

**Animaモデル対応**

- **モデルモード**: YAMLファイル単位で `SD系` / `Anima` を切り替え（`model_mode`）。未設定の既存ファイルは `SD系` として扱われます
- **自然言語アイテム**: 置換・エスケープをスキップし長文をそのまま出力するアイテムタイプ（`isNatural`）
- **カテゴリ整列**: グループにカテゴリ（品質/主体/キャラクター/作品/絵師/一般）を設定でき、AnimaモードではAnima推奨のタグ順序（quality → subject → character → series → artist → general）で自動整列して出力します。ツリーの表示順は変わりません
- **Animaテンプレート**: 新規ファイル作成時に「Animaテンプレートで作成」を選ぶと、カテゴリ設定済みのグループ雛形と公式推奨の品質タグ・ネガティブタグで初期化されます

**プロンプト翻訳**

- 自然言語アイテムの日本語原文を英訳して反映（原文は `sourceText` に保持され、出力には含まれません）
- ローカルLLM（LM Studio / Ollama 等）とクラウドAPI（OpenAI互換 / DeepL）に対応。`ローカル` / `クラウド` の2プロファイルをトグルで切替
- 翻訳設定（APIキー含む）はブラウザの `localStorage` にのみ保存され、サーバー側には残りません
- 接続テストボタン、モデル未検出等のサーバーエラー詳細表示

**PNG Info (infotext) 取込**

- 生成パラメータ文字列を貼り付けてプロンプトをツリー化。`Negative prompt:` 行と生成設定行を自動判別
- 強調構文 `(tag:1.2)` をウェイトへ変換、`((tag))` `[tag]` も `1.1^n` で概算
- エスケープ括弧の復元、`<lora:...>` / ワイルドカード / `BREAK` の保持、長文の自然言語アイテム化
- タグDBによるカテゴリ別グループ化、タグ名の日本語訳を「名前」へ設定するオプション

**自動グループ化 (タグDB / AI)**

- `a1111-sd-webui-tagcomplete` のタグDBを参照し、Danbooruのタグ種別からカテゴリを判定
- グループ編集モーダルの「カテゴリ自動判定」（多数決＋内訳表示）、サイドバーの「カテゴリ一括判定」（未設定グループのみ）
- **サブ分類による細分化**: 「一般」を髪・顔・体・服装・装飾品・ポーズ・構図・小物・背景・光/色・画風・効果・文字の13分類＋その他へ振り分け。右クリックメニューから任意のグループに対しても実行できます
- **AI補完**: ルールで判定できなかったタグのみローカルLLMへ問い合わせるハイブリッド方式

**反映と確認**

- **反映前プレビュー**: コンパイル結果全文と、現在のWebUIプロンプトとのタグ単位の差分（追加/削除/共通）を確認してから反映できます
- **トークン数の概算**: ペインヘッダーとプレビューに表示。SD系では75トークンのチャンク数も表示します（あくまで目安で、正確な数はモデルのトークナイザに依存します）

**整理作業の効率化**

- **移動先クイック選択**: 右クリックの「移動...」が検索付きダイアログになりました。最近使った移動先5件を先頭に固定、親パス併記で同名グループも判別でき、キーボード（↑↓ / Enter / Esc）で完結します

**表示・視認性**

- **BREAK区切りの強調表示**: `BREAK` のみのアイテムを横幅いっぱいの区切り線として表示
- **カテゴリバッジ**: グループに設定したカテゴリをヘッダに色分け表示（「一般」は非表示）
- **グループヘッダ背景色**: プリセット8色＋カラーピッカーによるカスタム色を設定可能
- **サイドバーの整理**: 設定項目を折りたたみパネルにグループ化し、項目が増えてもスクロールで収まるようにしました

### 変更 (Changed)

**ドラッグ&ドロップの作り直し**

長年やりづらかったDnDを見直しました。実装が2つの方式（SortableJSとHTML5ネイティブ）で混在しており、
PSMは `gradio-app` のshadowRoot内で動くためネイティブ側は実際には機能していませんでした。

- **SortableJSに統一**: 機能していなかったネイティブDnDハンドラを撤去し、共通設定を `src/dragOptions.ts` に集約
- **クローン方式へ変更**: ドラッグ中に移動元から要素が抜けないため、レイアウトが詰まって**ドロップ先がカーソルから逃げる**問題を解消しました
- **ドロップ精度の改善**: スクロールコンテナでのクリッピング防止、折り返しレイアウトでの挿入位置の安定化、ドラッグ中の自動スクロールに対応
- **空・狭いグループへのドロップ**: ドラッグ中は子要素の領域に最小高さを確保し、空グループは誘導ラベル付きのドロップ領域として表示します
- **視覚フィードバック**: ドラッグ中のアイテムを浮かせて表示し、挿入位置はゴースト、ドロップ先のグループは破線枠で示します

**その他**

- 移動操作を、グループが増えると探しづらかったサブメニューから検索付きダイアログへ変更
- 右クリックメニューの項目名が途中で省略されないよう幅を調整

### 修正 (Fixed)

- **アンダースコアを含むタグの破損（重要）**: プロンプト出力時の `_` → 半角スペース置換により、以下のタグが壊れていた問題を修正しました
  - スコアタグ: `score_7` → `score 7` となり品質指定が無効化されていた（Anima / Pony系）
  - 顔文字タグ: `^_^` `>_<` `@_@` などが `^ ^` のように変換されていた
  - 拡張ネットワーク構文: `<lora:my_style_v2:0.8>` のLoRA名が壊れ、読み込みに失敗していた
  - カンマ区切りで複数タグを含むアイテムでも、トークン単位で判定するようになりました
- ドラッグ中にグループへホバーしても自動展開されなかった問題（ネイティブdragイベントに依存していたため）
- ローディングオーバーレイがダイアログの背後に表示され、処理中でも操作できてしまっていた問題
- グループ編集モーダルのカテゴリ選択メニューがモーダルの裏に隠れて選べなかった問題
- ツールチップや入力欄のプレースホルダに、翻訳されていないキー名（`refresh` / `configDir` / `selectDir` / `open` / `clickToOpen`）が生表示されていた問題
- E2Eテストが合成 `drop` イベントを使っていたため、実際には動作しないDnDでもテストが通ってしまっていた問題（実マウス操作での検証に変更）

### 開発 (Development)

- **新規モジュール**
  - `scripts/psm/translate.py` — 翻訳プロバイダの抽象化（`openai` 互換 / `deepl`）。新規プロバイダは1関数の追加で対応可能
  - `scripts/psm/tagdb.py` — タグDBの参照とサブ分類ルール（約17万エントリを0.14秒でロード、プロセス内キャッシュ）
  - `src/dragOptions.ts` — DnDの共通オプションを集約
- **APIエンドポイント追加**: `/psm/translate`, `/psm/tag-categories`, `/psm/tagdb-status`
- **データ構造**: `PsmItem` に `isNatural` / `sourceText` / `category` / `headerColor` を追加（すべて省略可能）。YAMLルートに `model_mode` を追加
- **前方互換**: `storage.py` が保存時に未知のルートキーを保持するようになり、将来バージョンで作成されたファイルを開いても情報が失われません
- **テスト**: pytest に `test_psm_translate.py` / `test_psm_tagdb.py` を追加（計約74件）。Vitest は翻訳・差分計算・トークン数・PNG Info取込・移動・DnDを追加し約112件に
- **ドキュメント**: `CHANGELOG.md` を新設。`doc/ANIMA_SUPPORT.md` に調査・設計の経緯を記録

## [1.3.0] 以前

初期リリースから、階層構造管理・プロファイル・重複チェック・グループマップ・プロンプト大辞典連携などを実装。
