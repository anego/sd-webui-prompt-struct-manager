# 変更履歴 (Changelog)

このプロジェクトの主な変更点を記録します。

## [1.4.0] - 2026-07-25

Animaモデル対応、プロンプト翻訳、PNG Info取込、自動グループ化を追加した大型アップデートです。
既存のYAMLファイルはそのまま利用でき、追加フィールドはすべて省略可能です（後方互換）。

### 追加 (Added)

**Animaモデル対応**

- **モデルモード**: YAMLファイル単位で `SD系` / `Anima` を切り替え（`model_mode`）。未設定の既存ファイルは `SD系` として扱われます
- **自然言語アイテム**: 置換・エスケープをスキップし長文をそのまま出力するアイテムタイプ（`isNatural`）
- **カテゴリ整列**: グループにカテゴリ（品質/主体/キャラクター/作品/絵師/一般）を設定でき、AnimaモードではAnima推奨のタグ順序で自動整列して出力します
- **カテゴリバッジ**: 設定したカテゴリをグループヘッダに色分け表示（「一般」は非表示）
- **Animaテンプレート**: 新規ファイル作成時に推奨タグ入りのカテゴリ別グループ雛形で初期化

**プロンプト翻訳**

- 自然言語アイテムの日本語原文を英訳して反映（原文は `sourceText` に保持され、出力には含まれません）
- ローカルLLM（LM Studio / Ollama 等）とクラウドAPI（OpenAI互換 / DeepL）に対応。`ローカル` / `クラウド` の2プロファイルをトグルで切替
- 翻訳設定（APIキー含む）はブラウザの `localStorage` にのみ保存され、サーバー側には残りません
- 接続テストボタン、モデル未検出等のサーバーエラー詳細表示

**PNG Info (infotext) 取込**

- 生成パラメータ文字列を貼り付けてプロンプトをツリー化。`Negative prompt:` 行と生成設定行を自動判別
- 強調構文 `(tag:1.2)` をウェイトへ変換、`((tag))` `[tag]` も `1.1^n` で概算
- エスケープ括弧の復元、`<lora:...>` / ワイルドカード / `BREAK` の保持、長文の自然言語アイテム化
- タグDBによるカテゴリ別グループ化、タグ名の日本語訳を「名前」へ設定するオプション

**自動グループ化 (タグDB / AI)**

- `a1111-sd-webui-tagcomplete` のタグDBを参照し、Danbooruのタグ種別からカテゴリを判定
- グループ編集モーダルの「カテゴリ自動判定」（多数決＋内訳表示）、サイドバーの「カテゴリ一括判定」（未設定グループのみ）
- **サブ分類による細分化**: 「一般」を髪・顔・体・服装・装飾品・ポーズ・構図・小物・背景・光/色・画風・効果・文字の13分類＋その他へ振り分け。右クリックメニューから任意のグループに対しても実行できます
- **AI補完**: ルールで判定できなかったタグのみローカルLLMへ問い合わせるハイブリッド方式

**その他の機能**

- **反映前プレビュー**: コンパイル結果全文と、現在のWebUIプロンプトとのタグ単位の差分（追加/削除/共通）を確認してから反映
- **トークン数の概算**: ペインヘッダーとプレビューに表示。SD系では75トークンのチャンク数も表示
- **移動先クイック選択**: 検索付きダイアログ、最近使った移動先5件、親パス表示、キーボード操作（↑↓/Enter/Esc）
- **BREAK区切りの強調表示**: `BREAK` を横幅いっぱいの区切り線として表示
- **グループヘッダ背景色**: プリセット8色＋カスタム色を設定可能
- サイドバーの折りたたみグループ化とスクロール対応

### 変更 (Changed)

- **ドラッグ&ドロップをSortableJSに統一**: shadowRoot環境で機能していなかったネイティブDnDハンドラを撤去し、共通設定を `src/dragOptions.ts` に集約
- **クローン方式に変更**: ドラッグ中に移動元から要素が抜けないため、レイアウトが詰まってドロップ先が逃げる問題を解消
- ドロップ精度の改善（`fallbackOnBody` / `emptyInsertThreshold` / `swapThreshold` / `invertSwap` / 自動スクロール）とドラッグ中の視覚フィードバック追加
- 空・狭いグループへのドロップ改善（ドラッグ中に最小高さを確保し、空グループは誘導ラベル付きのドロップ領域として表示）
- 移動操作をサブメニューから検索付きダイアログへ変更
- 右クリックメニューの項目名が省略されないよう幅を調整

### 修正 (Fixed)

- **スコアタグの破損**: `score_7` が `score 7` に変換され無効化される問題を修正。あわせて顔文字タグ（`^_^`, `>_<` 等）と拡張ネットワーク構文（`<lora:name_v2:0.8>`）のアンダースコアも保護されるようになりました
- ドラッグ中のグループ自動展開が機能していなかった問題（ネイティブdragイベント依存だったため）
- ローディングオーバーレイがダイアログの背後に表示される問題
- グループ編集モーダルのカテゴリ選択メニューがモーダルの裏に隠れる問題
- 未定義だったi18nキー（`refresh` / `configDir` / `selectDir` / `open` / `clickToOpen`）によりツールチップにキー名が生表示されていた問題
- E2Eテストが合成 `drop` イベントを使用しており、実際には動作しないDnDでもテストが通っていた問題

### 開発 (Development)

- バックエンドに `scripts/psm/translate.py`（翻訳プロバイダ抽象化）、`scripts/psm/tagdb.py`（タグDB・サブ分類）を追加
- APIエンドポイント追加: `/psm/translate`, `/psm/tag-categories`, `/psm/tagdb-status`
- テスト追加: pytest（`test_psm_translate.py` / `test_psm_tagdb.py`）、Vitest（翻訳・差分・トークン数・取込・移動・DnD）
- `storage.py` が保存時に未知のルートキーを保持するようになり、将来バージョンとの相互運用性が向上

## [1.3.0] 以前

初期リリースから、階層構造管理・プロファイル・重複チェック・グループマップ・プロンプト大辞典連携などを実装。
